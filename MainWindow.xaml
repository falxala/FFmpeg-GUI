<Window x:Class="ffmpeg.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FFmpeg GUI" Height="700" Width="900">

    <!-- ここからがウィンドウ全体のGridです -->
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Status Text -->
            <RowDefinition Height="10"/>
            <!-- Spacer -->
            <RowDefinition Height="Auto"/>
            <!-- Input Buttons -->
            <RowDefinition Height="*"/>
            <!-- File List -->
            <RowDefinition Height="Auto"/>
            <!-- List Controls -->
            <RowDefinition Height="10"/>
            <!-- Spacer -->
            <RowDefinition Height="Auto"/>
            <!-- Conversion Controls -->
            <RowDefinition Height="10"/>
            <!-- Spacer -->
            <RowDefinition Height="150"/>
            <!-- FFmpeg Output Log -->
        </Grid.RowDefinitions>

        <!-- FFmpeg Status (Grid.Row 0) -->
        <TextBlock x:Name="StatusTextBlock" Grid.Row="0" TextWrapping="Wrap" 
                   Margin="0,0,0,10" VerticalAlignment="Top" HorizontalAlignment="Left"
                   Text="FFmpegの存在を確認中..."/>

        <!-- プログレスバーはここから削除されました -->

        <!-- ファイル入力コントロール (Grid.Row 2) -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
            <Button x:Name="SelectFileButton" Content="ファイルを選択..." Click="SelectFileButton_Click" Width="120" Height="30" Margin="0,0,10,0"/>
            <Button x:Name="SelectFolderButton" Content="フォルダを選択..." Click="SelectFolderButton_Click" Width="120" Height="30" Margin="0,0,10,0"/>
        </StackPanel>

        <!-- ドロップ領域とファイルリスト (Grid.Row 3) -->
        <Border Grid.Row="3" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Padding="5" 
                AllowDrop="True" Drop="FileList_Drop" DragOver="FileList_DragOver" Margin="0,0,0,10">
            <ListView x:Name="FileList" ItemsSource="{Binding SourceFiles}" SelectionMode="Extended">
                <ListView.View>
                    <GridView>
                        <!-- チェックボックス列 -->
                        <GridViewColumn Width="40">
                            <GridViewColumn.HeaderTemplate>
                                <DataTemplate>
                                    <CheckBox x:Name="SelectAllCheckBox" 
                                              Checked="SelectAllCheckBox_Checked" 
                                              Unchecked="SelectAllCheckBox_Unchecked" />
                                </DataTemplate>
                            </GridViewColumn.HeaderTemplate>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- 既存の列 (Widthを固定値に修正) -->
                        <GridViewColumn Header="ファイル名" DisplayMemberBinding="{Binding FileName}" Width="200"/>
                        <GridViewColumn Header="時間" DisplayMemberBinding="{Binding Duration}" Width="80"/>
                        <GridViewColumn Header="映像コーデック" DisplayMemberBinding="{Binding VideoCodec}" Width="100"/>
                        <GridViewColumn Header="音声コーデック" DisplayMemberBinding="{Binding AudioCodec}" Width="100"/>

                        <!-- 状態列 (進捗列は削除) -->
                        <GridViewColumn Header="状態" DisplayMemberBinding="{Binding Status}" Width="150"/>

                        <!-- フルパス -->
                        <GridViewColumn Header="フルパス" DisplayMemberBinding="{Binding FullPath}" Width="300"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- リスト操作ボタン (Grid.Row 4) -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="RemoveSelectedButton" Content="選択を削除" Click="RemoveSelectedButton_Click" Width="100" Height="30" Margin="0,0,10,0"/>
            <Button x:Name="ClearListButton" Content="全てクリア" Click="ClearListButton_Click" Width="100" Height="30"/>
        </StackPanel>

        <!-- 変換コントロール (Grid.Row 6) -->
        <Grid Grid.Row="6" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 出力先設定 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="出力先フォルダ: " VerticalAlignment="Center"/>
                <TextBox x:Name="OutputFolderTextBox" Width="300" Margin="5,0" VerticalAlignment="Center" IsReadOnly="True"/>
                <Button x:Name="SelectOutputFolderButton" Content="..." Click="SelectOutputFolderButton_Click" Width="40" Height="24"/>
            </StackPanel>

            <!-- 変換開始ボタン -->
            <Button x:Name="StartConversionButton" Grid.Column="1" Content="変換開始" Width="150" Height="40" 
                    HorizontalAlignment="Right" Click="StartConversionButton_Click" />
        </Grid>

        <!-- FFmpeg 生出力ログ (Grid.Row 8) -->
        <Border Grid.Row="8" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Padding="5" Margin="0,10,0,0">
            <TextBox x:Name="FFmpegOutputLog"
                     IsReadOnly="True"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas, Meiryo"
                     Text="ここにFFmpegの出力が表示されます..."/>
        </Border>

    </Grid>
    <!-- ここがウィンドウ全体のGridの終了タグです -->

</Window>